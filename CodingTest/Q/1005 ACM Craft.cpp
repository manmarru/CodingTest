#include "pch.h"
#include "Header.h"

void Solve(ifstream* _pLoadStream)
{
	int CaseSize;
	int NodeSize;
	int LinkSize;
	CIN >> CaseSize >> NodeSize >> LinkSize;

	for (int i = 0; i < LinkSize; ++i)
	{

	}
}