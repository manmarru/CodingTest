#include "pch.h"
#include "Header.h"
#include <vector>

vector<int> Inputs;

void MergeSort(int Start, int End)
{
	if (End - Start < 2)
	{
		return;
	}

	int Mid = (Start + Mid) / 2;


	
}

void Solve(ifstream* _pLoadStream)
{
	int InputSize, Objection;
	CIN >> InputSize >> Objection;
	Inputs.resize(InputSize);
	for (int i = 0; i < InputSize; ++i)
	{
		CIN >> Inputs[i];
	}

	
}