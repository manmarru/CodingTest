#pragma once
#include "pch.h"
#include "Header.h"

void Solve(ifstream* pLoadStream)
{
	입출력최적화;
	/*
	n개 카드뭉치를
	지우고
	맨 아래로 보내는걸 반복
	*/
	int iInput;
	*pLoadStream >> iInput;

	if (iInput == 1)
	{
		cout << 1 << endl;
		return;
	}


}